import { ComingSoon } from "@/components/ui/coming-soon";

export default function ResidencePage() {
  return (
    <ComingSoon
      title="Accommodation"
      message="Accommodation registrations are now closed due to full occupancy."
    />
  );
}
